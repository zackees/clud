# Optional messaging dependencies for CLUD
# Install with: pip install -r src/clud/messaging/requirements.txt

# Telegram support
python-telegram-bot>=20.0

# SMS support (Twilio)
twilio>=8.0.0

# WhatsApp and HTTP support
requests>=2.31.0

# Note: AWS SNS requires boto3 (already in main dependencies if using AWS)
