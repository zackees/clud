# Example Environment Variables for Telegram Integration
# Copy this file to .env and fill in your values

# ============================================================================
# Required Configuration
# ============================================================================

# Telegram bot token from @BotFather
# Get one by messaging @BotFather on Telegram and creating a new bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ============================================================================
# Web Interface Configuration (Optional)
# ============================================================================

# Port for the web interface (default: 8889)
TELEGRAM_WEB_PORT=8889

# Authentication token for web access (optional)
# If set, web clients must provide this token to connect
# TELEGRAM_WEB_AUTH=your_secret_token_here

# ============================================================================
# Session Management (Optional)
# ============================================================================

# Session timeout in seconds (default: 3600 = 1 hour)
TELEGRAM_SESSION_TIMEOUT=3600

# Maximum number of concurrent sessions (default: 50)
TELEGRAM_MAX_SESSIONS=50

# Maximum messages to keep in history per session (default: 1000)
TELEGRAM_MESSAGE_HISTORY_LIMIT=1000

# ============================================================================
# Telegram Bot Configuration (Optional)
# ============================================================================

# Comma-separated list of allowed Telegram user IDs (optional)
# Leave empty to allow all users
# Example: TELEGRAM_ALLOWED_USERS=123456789,987654321
TELEGRAM_ALLOWED_USERS=

# Webhook URL for production deployments (optional)
# Leave empty to use polling mode (recommended for development)
# TELEGRAM_WEBHOOK_URL=https://yourdomain.com/telegram/webhook

# ============================================================================
# Claude Code Configuration (Optional)
# ============================================================================

# Claude model to use (default: claude-sonnet-4.5)
# ANTHROPIC_MODEL=claude-sonnet-4.5

# ============================================================================
# Logging Configuration (Optional)
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (optional, logs to console if not set)
# LOG_FILE=logs/telegram.log

# ============================================================================
# Advanced Configuration (Optional)
# ============================================================================

# Maximum number of instances in the pool (default: 100)
# INSTANCE_POOL_MAX_INSTANCES=100

# Instance idle timeout in seconds (default: 1800 = 30 minutes)
# INSTANCE_POOL_IDLE_TIMEOUT=1800

# Enable debug mode for verbose logging
# DEBUG=false

# Enable performance metrics collection
# METRICS_ENABLED=false

# ============================================================================
# Notes
# ============================================================================

# 1. Environment variables take precedence over configuration file settings
# 2. You can use ${VAR_NAME} syntax in telegram_config.yaml to reference these
# 3. Never commit this file with real tokens/secrets to version control
# 4. Use a secrets manager for production deployments
# 5. Use `clud --login` to configure your Anthropic API key
