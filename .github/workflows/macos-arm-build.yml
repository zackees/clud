name: macOS ARM Build
on:
  push:
    branches: [rust, main]
  pull_request:
jobs:
  call:
    uses: ./.github/workflows/_build.yml
    with:
      runs-on: macos-15
      artifact-name: wheels-macos-arm
      build-mode: dev
